# replace the value of ANTHROPIC_API_KEY with your actual API key from Anthropic. Do not share this key publicly or commit it to version control.
# rename to `.env` and ensure it is included in your .gitignore file to keep it secure.

ANTHROPIC_API_KEY=your_anthropic_api_key_here